# ShopSphere cart service

Owns the cart bounded context. Domain models, controllers, routes, middleware, validators, utilities, and service-specific logic live under \.

Cross-service communication must use HTTP APIs/service clients, never another service's models or database code.
