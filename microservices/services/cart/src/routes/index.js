const router=require('express').Router();const c=require('../controllers/cart.controller');const {authenticate,authorize}=require('../middleware/auth');const internal=require('../middleware/internal');
router.get('/api/cart',authenticate,authorize('USER'),c.getCart);router.post('/api/cart',authenticate,authorize('USER'),c.add);router.put('/api/cart/:id',authenticate,authorize('USER'),c.update);router.delete('/api/cart/:id',authenticate,authorize('USER'),c.remove);router.delete('/api/cart',authenticate,authorize('USER'),c.clear);router.get('/internal/cart/:userId',internal,c.internalCart);router.post('/internal/cart/:userId/clear',internal,c.internalClear);module.exports=router;
